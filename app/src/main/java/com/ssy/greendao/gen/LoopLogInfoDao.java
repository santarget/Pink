package com.ssy.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.ssy.pink.bean.weibo.LoopLogInfo;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "LOOP_LOG_INFO".
*/
public class LoopLogInfoDao extends AbstractDao<LoopLogInfo, Long> {

    public static final String TABLENAME = "LOOP_LOG_INFO";

    /**
     * Properties of entity LoopLogInfo.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property LogId = new Property(0, Long.class, "logId", true, "_id");
        public final static Property Time = new Property(1, String.class, "time", false, "TIME");
        public final static Property WeiboUid = new Property(2, String.class, "weiboUid", false, "WEIBO_UID");
        public final static Property Log = new Property(3, String.class, "log", false, "LOG");
    }


    public LoopLogInfoDao(DaoConfig config) {
        super(config);
    }
    
    public LoopLogInfoDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"LOOP_LOG_INFO\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT UNIQUE ," + // 0: logId
                "\"TIME\" TEXT," + // 1: time
                "\"WEIBO_UID\" TEXT," + // 2: weiboUid
                "\"LOG\" TEXT);"); // 3: log
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"LOOP_LOG_INFO\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, LoopLogInfo entity) {
        stmt.clearBindings();
 
        Long logId = entity.getLogId();
        if (logId != null) {
            stmt.bindLong(1, logId);
        }
 
        String time = entity.getTime();
        if (time != null) {
            stmt.bindString(2, time);
        }
 
        String weiboUid = entity.getWeiboUid();
        if (weiboUid != null) {
            stmt.bindString(3, weiboUid);
        }
 
        String log = entity.getLog();
        if (log != null) {
            stmt.bindString(4, log);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, LoopLogInfo entity) {
        stmt.clearBindings();
 
        Long logId = entity.getLogId();
        if (logId != null) {
            stmt.bindLong(1, logId);
        }
 
        String time = entity.getTime();
        if (time != null) {
            stmt.bindString(2, time);
        }
 
        String weiboUid = entity.getWeiboUid();
        if (weiboUid != null) {
            stmt.bindString(3, weiboUid);
        }
 
        String log = entity.getLog();
        if (log != null) {
            stmt.bindString(4, log);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public LoopLogInfo readEntity(Cursor cursor, int offset) {
        LoopLogInfo entity = new LoopLogInfo( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // logId
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // time
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // weiboUid
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // log
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, LoopLogInfo entity, int offset) {
        entity.setLogId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setTime(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setWeiboUid(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setLog(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(LoopLogInfo entity, long rowId) {
        entity.setLogId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(LoopLogInfo entity) {
        if(entity != null) {
            return entity.getLogId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(LoopLogInfo entity) {
        return entity.getLogId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
