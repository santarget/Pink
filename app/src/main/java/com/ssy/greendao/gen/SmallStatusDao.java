package com.ssy.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.ssy.pink.bean.SmallStatus;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SMALL_STATUS".
*/
public class SmallStatusDao extends AbstractDao<SmallStatus, String> {

    public static final String TABLENAME = "SMALL_STATUS";

    /**
     * Properties of entity SmallStatus.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Uid = new Property(0, String.class, "uid", true, "UID");
        public final static Property Normal = new Property(1, boolean.class, "normal", false, "NORMAL");
    }


    public SmallStatusDao(DaoConfig config) {
        super(config);
    }
    
    public SmallStatusDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SMALL_STATUS\" (" + //
                "\"UID\" TEXT PRIMARY KEY NOT NULL UNIQUE ," + // 0: uid
                "\"NORMAL\" INTEGER NOT NULL );"); // 1: normal
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SMALL_STATUS\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, SmallStatus entity) {
        stmt.clearBindings();
 
        String uid = entity.getUid();
        if (uid != null) {
            stmt.bindString(1, uid);
        }
        stmt.bindLong(2, entity.getNormal() ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, SmallStatus entity) {
        stmt.clearBindings();
 
        String uid = entity.getUid();
        if (uid != null) {
            stmt.bindString(1, uid);
        }
        stmt.bindLong(2, entity.getNormal() ? 1L: 0L);
    }

    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0);
    }    

    @Override
    public SmallStatus readEntity(Cursor cursor, int offset) {
        SmallStatus entity = new SmallStatus( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // uid
            cursor.getShort(offset + 1) != 0 // normal
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, SmallStatus entity, int offset) {
        entity.setUid(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setNormal(cursor.getShort(offset + 1) != 0);
     }
    
    @Override
    protected final String updateKeyAfterInsert(SmallStatus entity, long rowId) {
        return entity.getUid();
    }
    
    @Override
    public String getKey(SmallStatus entity) {
        if(entity != null) {
            return entity.getUid();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(SmallStatus entity) {
        return entity.getUid() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
